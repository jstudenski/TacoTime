<div class="taco">
  <div class="taco-title">
  <p>{{this.taco_name}}</p>
  </div>
  {{#if eaten}}
    <div data-id="{{this.id}}" class="taco-button deleteTaco"><p>X</p></div>
  {{else}}
    <div data-id="{{this.id}}" class="taco-button eatTaco"><p>EAT</p></div>
  {{/if}}
</div>